[package]
name = "snark-precompiled-artifact-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
snark = { path = "../../.." }

[build-dependencies]
snark = { path = "../../.." }

[workspace]
